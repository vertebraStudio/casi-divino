---
interface Props {
  title: string;
  subtitle: string;
  ctaLabel: string;
  ctaHref: string;
}

const { title, subtitle, ctaHref, ctaLabel } = Astro.props;
---

<section class="relative overflow-hidden border-b border-slate-800/40 bg-slate-950">
  <!-- Imagen de fondo -->
  <div
    class="pointer-events-none absolute inset-0 bg-cover bg-center bg-no-repeat"
    style="background-image: url('/media/fondoprueba.jpg');"
    aria-hidden="true"
  />
  <div class="pointer-events-none absolute inset-0 bg-gradient-to-b from-black/30 via-slate-950/40 to-black/60" />

  <div class="relative mx-auto flex min-h-screen max-w-5xl flex-col gap-10 px-4 py-16 sm:px-6 sm:py-20 lg:flex-row lg:items-center">
    <div class="relative z-10 max-w-xl space-y-6 rounded-3xl bg-white/5 p-6 shadow-2xl shadow-black/40 ring-1 ring-white/10 backdrop-blur-xl sm:p-7 lg:p-8">
      <p class="inline-flex items-center rounded-full border border-vino-500/60 bg-black/40 px-3 py-1 text-xs font-medium uppercase tracking-wide text-vino-100 shadow-sm backdrop-blur">
        Guías, reseñas y comparativas de vino
      </p>
      <h1 class="text-4xl font-serif font-semibold tracking-tight text-slate-50 sm:text-5xl lg:text-6xl">
        {title}
      </h1>
      <p class="text-lg leading-[2.6rem] text-slate-300 sm:text-xl">
        {subtitle}
      </p>
      <div class="flex flex-wrap items-center gap-3">
        <a
          href={ctaHref}
          class="inline-flex items-center justify-center rounded-full bg-vino-500/90 px-6 py-2.5 text-sm font-semibold text-white shadow-lg shadow-vino-900/50 transition hover:bg-vino-400 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-vino-400 focus-visible:ring-offset-2 focus-visible:ring-offset-slate-950"
        >
          {ctaLabel}
        </a>
        <p class="text-xs text-slate-400">
          Selección curada de accesorios y vinotecas para amantes del vino.
        </p>
      </div>
    </div>

    <div class="relative z-10 flex-1">
      <div class="mx-auto max-w-sm rounded-3xl bg-black/40 p-4 shadow-2xl shadow-black/60 ring-1 ring-white/10 backdrop-blur-2xl">
        <div class="aspect-[4/5] overflow-hidden rounded-2xl border border-white/10 bg-gradient-to-tr from-vino-900/90 via-vino-700/80 to-vino-500/80">
          <div class="flex h-full flex-col justify-between p-4 text-xs text-slate-100">
            <div class="space-y-2">
              <div class="inline-flex items-center gap-1 rounded-full bg-black/40 px-2 py-1 text-[10px] uppercase tracking-wide text-vino-100">
                Placeholder imagen vinoteca / copas
              </div>
              <div class="h-5 w-24 rounded-full bg-white/15" />
              <div class="h-4 w-32 rounded-full bg-white/10" />
            </div>
            <div class="space-y-2">
              <div class="h-3 w-28 rounded-full bg-white/15" />
              <div class="h-3 w-32 rounded-full bg-white/10" />
              <div class="h-3 w-20 rounded-full bg-white/15" />
            </div>
          </div>
        </div>
        <p class="mt-3 text-xs text-slate-300">
          Sustituye esta tarjeta por una fotografía de alta calidad de copas de vino o una vinoteca,
          manteniendo el efecto de cristal esmerilado.
        </p>
      </div>
    </div>
  </div>
</section>


