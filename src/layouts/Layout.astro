---
import '../styles/global.css';

import Header from '../components/Header.astro';
import AffiliateDisclaimer from '../components/AffiliateDisclaimer.astro';
import SEOHead from '../components/SEOHead.astro';

interface Props {
  title: string;
  description: string;
  canonical?: string;
  ogImage?: string;
  noindex?: boolean;
}

const { title, description, canonical, ogImage, noindex } = Astro.props;
---

<!doctype html>
<html lang="es" class="h-full scroll-smooth dark">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link
      rel="preconnect"
      href="https://fonts.googleapis.com"
    />
    <link
      rel="preconnect"
      href="https://fonts.gstatic.com"
      crossorigin
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Lato:wght@300;400;500;600;700&family=Playfair+Display:wght@500;600;700&display=swap"
      rel="stylesheet"
    />
    <SEOHead
      {title}
      {description}
      {canonical}
      {ogImage}
      {noindex}
    />
  </head>

  <body class="flex min-h-full flex-col bg-white text-slate-900 dark:bg-slate-950 dark:text-slate-50">
    <Header />

    <main class="flex-1">
      <slot />
    </main>

    <footer class="border-t border-slate-200 bg-white/90 py-6 text-xs text-slate-500 dark:border-slate-800 dark:bg-slate-950/90 dark:text-slate-400">
      <div class="mx-auto flex max-w-5xl flex-col gap-4 px-4 sm:flex-row sm:items-center sm:justify-between sm:px-6">
        <div class="space-y-1">
          <p class="font-serif text-sm font-semibold text-slate-800 dark:text-slate-100">
            Casi Divino
          </p>
          <AffiliateDisclaimer />
        </div>
        <div class="space-y-1 text-[11px] text-right sm:text-xs">
          <p>Contenido con fines informativos. Bebe siempre con responsabilidad.</p>
          <p>© {new Date().getFullYear()} Casi Divino. Todos los derechos reservados.</p>
        </div>
      </div>
    </footer>
  </body>
</html>


